# Copie para .env e preencha valores locais. NUNCA versione o .env (ja esta no .gitignore).
DATA_DIR=data
OUTPUT_DIR=outputs

# Camada de IA (opcional). Sem chave, as explicacoes usam fallback offline deterministico.
# Provedor padrao: OpenAI. Use anthropic so se quiser trocar.
LLM_PROVIDER=openai
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# ANTHROPIC_API_KEY=
